apply plugin: 'java'

task wrapper(type: Wrapper) {
  gradleVersion = '2.4'
}

sourceSets.main.java.exclude 'usbrubberducky/encoder/Encoder_GUI.java'

jar {
  archiveName = 'encoder.jar'
  manifest {
    from 'src/main/resources/META-INF/MANIFEST.MF'
  }
}

apply from: 'test.gradle'
